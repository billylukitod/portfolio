---
interface Props {
  class?: string;
  wide?: boolean;
}
const { class: className = '', wide = false } = Astro.props;
---
<div class:list={['mx-auto px-4 sm:px-6 lg:px-8 w-full', wide ? 'max-w-[var(--width-content-max)]' : 'max-w-[var(--width-content-wide)]', className]}>
  <slot />
</div>
